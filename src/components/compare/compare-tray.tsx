// Functional stub. U7 will implement the full compare tray here.
//
// Contract (intended):
//   - A persistent, slide-up tray that collects cars the user has flagged to
//     compare, with a CTA to open /compare.
//   - Likely backed by a shared "compare selection" store/hook owned by U7.
//
// TODO(U7): replace this stub with the real CompareTray implementation.
export function CompareTray() {
  return null
}
